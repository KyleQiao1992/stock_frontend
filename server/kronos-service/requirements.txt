# Kronos 推理服务依赖。
# 注意：torch 的安装方式取决于你的平台（CPU / CUDA）。
# CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA: 见 https://pytorch.org/get-started/locally/
#
# Kronos 模型代码（model 包）来自其仓库，不在 PyPI：
#   git clone https://github.com/shiyu-coder/Kronos
# 然后把 Kronos 仓库目录加入 PYTHONPATH，或在其中 pip install -e .

fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
pydantic>=2.0
huggingface_hub>=0.23
einops
# torch  -> 按上面平台说明单独安装
# Kronos -> 见上方 git clone 说明
